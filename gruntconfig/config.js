'use strict';

var config = {
  build: '.build',
  buildPort: 8510,
  dist: 'dist',
  distPort: 8513,
  example: 'example',
  examplePort: 8512,
  liveReloadPort: 8519,
  src: 'src',
  templatePort: 8518,
  test: 'test',
  testPort: 8511
};

module.exports = config;
